"""Pure domain logic with no I/O or framework dependencies.

Everything here is deterministic and unit-testable in isolation: identifier
formatting, cryptographic hashing helpers, the file-state machine, and
path-safety checks.
"""
